% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zusmt.R
\name{smt_unsat_core}
\alias{smt_unsat_core}
\title{The unsat core of an unsatisfiable problem}
\usage{
smt_unsat_core(solver, named_only = FALSE)
}
\arguments{
\item{solver}{A solver from \code{\link[=smt_solver]{smt_solver()}}, created with
\code{unsat_cores = TRUE}, on which \code{\link[=smt_check]{smt_check()}} has returned \code{"unsat"}.}

\item{named_only}{Whether to report only assertions named with
\verb{(! ... :named n)}, which is what SMT-LIB means by an unsat core and what
the \code{(get-unsat-core)} command returns. \code{FALSE}, the default, reports the
core's assertions whether or not they were named -- an SMT-LIB-conformant
core of a script that names nothing is empty, which is rarely the answer
an R caller wants.}
}
\value{
A character vector of assertions in the solver's own printed form,
or of names when \code{named_only = TRUE}. If any assertion in the core was
named, the names come back as the vector's \code{names()}.
}
\description{
The subset of the assertions that is already unsatisfiable on its own --
the solver's explanation of why the answer was \code{"unsat"}.
}
\details{
Requires a solver created with \code{smt_solver(unsat_cores = TRUE)}: whether a
proof is recorded is fixed when the solver is built, so it cannot be turned
on after the fact.
}
\examples{
s <- smt_solver("QF_LIA", unsat_cores = TRUE)
smt_assert(s, "
  (declare-const x Int)
  (assert (! (> x 5) :named lower))
  (assert (! (< x 3) :named upper))
  (assert (! (= x 4) :named unrelated))
")
smt_check(s)
smt_unsat_core(s)
smt_unsat_core(s, named_only = TRUE)
}
\seealso{
\code{\link[=smt_solver]{smt_solver()}}, \code{\link[=smt_check]{smt_check()}}
}
