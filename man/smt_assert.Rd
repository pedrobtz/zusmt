% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zusmt.R
\name{smt_assert}
\alias{smt_assert}
\title{Send SMT-LIB input to a solver}
\usage{
smt_assert(solver, text)
}
\arguments{
\item{solver}{A solver from \code{\link[=smt_solver]{smt_solver()}}.}

\item{text}{A single string of SMT-LIB2 input. Newlines are fine, and
several commands may appear in one call.}
}
\value{
The solver, invisibly, so calls can be chained.
}
\description{
Runs SMT-LIB2 text through the bundled solver. Despite the name, any
SMT-LIB2 commands are accepted, not only \code{assert} — declarations, \code{push} and
\code{pop}, options, and so on — because the package is a front end to the
solver's own language rather than a curated subset of it.
}
\details{
Use \code{\link[=smt_check]{smt_check()}} rather than a \code{(check-sat)} command: it returns the result
to R instead of printing it.
}
\examples{
s <- smt_solver("QF_LRA")
smt_assert(s, "
  (declare-const x Real)
  (declare-const y Real)
  (assert (> x y))
  (assert (> y 0.0))
")
smt_check(s)
}
