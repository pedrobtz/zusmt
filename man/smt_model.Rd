% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zusmt.R
\name{smt_model}
\alias{smt_model}
\title{The model of a satisfiable problem}
\usage{
smt_model(solver)
}
\arguments{
\item{solver}{A solver from \code{\link[=smt_solver]{smt_solver()}}, on which \code{\link[=smt_check]{smt_check()}} has just
returned \code{"sat"}.}
}
\value{
A named list, one element per 0-ary declaration. Booleans come back
as logicals and numbers as doubles carrying an \code{"exact"} attribute with
the solver's exact rational, since an SMT rational need not be
representable as a double. Values of other sorts come back as the solver's
own printed form.

Functions of arity greater than zero are omitted: they have no single
value to report.
}
\description{
The model of a satisfiable problem
}
\examples{
s <- smt_solver("QF_LIA")
smt_assert(s, "(declare-const x Int) (assert (= x 42))")
smt_check(s)
smt_model(s)
}
