% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zusmt.R
\name{smt_interpolant}
\alias{smt_interpolant}
\title{A Craig interpolant between two groups of assertions}
\usage{
smt_interpolant(solver, a)
}
\arguments{
\item{solver}{A solver from \code{\link[=smt_solver]{smt_solver()}}, created with
\code{interpolants = TRUE}, on which \code{\link[=smt_check]{smt_check()}} has returned \code{"unsat"}.}

\item{a}{Names of the assertions forming group \code{A}, as given with
\verb{(! ... :named n)}. Every other assertion forms \code{B}.}
}
\value{
A character vector of interpolants in the solver's printed form,
usually of length one.
}
\description{
Given assertions that are unsatisfiable together, split into a group \code{A}
and everything else \code{B}, an interpolant is a formula that follows from \code{A},
contradicts \code{B}, and mentions only the symbols \code{A} and \code{B} share. It is a
summary of why the two conflict, in their shared vocabulary -- which is
what makes it useful for abstraction and for explaining a contradiction in
terms neither side owns alone.
}
\details{
Requires a solver created with \code{smt_solver(interpolants = TRUE)}, since
whether a proof is recorded is fixed when the solver is built.
}
\examples{
s <- smt_solver("QF_LIA", interpolants = TRUE)
smt_assert(s, "
  (declare-const x Int)
  (declare-const y Int)
  (assert (! (and (> x 5) (= y x)) :named A))
  (assert (! (< y 3) :named B))
")
smt_check(s)
smt_interpolant(s, "A")
}
\seealso{
\code{\link[=smt_solver]{smt_solver()}}, \code{\link[=smt_unsat_core]{smt_unsat_core()}}
}
